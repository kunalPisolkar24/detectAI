name: Service - Payment Gateway

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - staging
    paths:
      - "services/payments/gateway/**"
      - ".github/workflows/service-payment-gateway.yaml"
  pull_request:
    branches:
      - main
      - staging
    paths:
      - "services/payments/gateway/**"
      - ".github/workflows/service-payment-gateway.yaml"

env:
  DOCKER_IMAGE_NAME: kunalpisolkar/detect-ai-payment-gateway
  SERVICE_PATH: ./services/payments/gateway

jobs:
  quality-assurance:
    name: Run Tests & Lint
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ${{ env.SERVICE_PATH }}

    services:
      rabbitmq:
        image: rabbitmq:3.12-management-alpine
        env:
          RABBITMQ_DEFAULT_USER: guest
          RABBITMQ_DEFAULT_PASS: guest
        ports:
          - 5672:5672
        options: >-
          --health-cmd "rabbitmq-diagnostics -q ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25'
          cache-dependency-path: ${{ env.SERVICE_PATH }}/go.sum

      - name: Install Dependencies
        run: go mod download

      - name: Verify Code
        run: go vet ./...

      - name: Run Unit Tests
        run: go test $(go list ./... | grep -v '/test/integration') -v -cover

      - name: Run Integration Tests
        env:
          TEST_RABBITMQ_URL: amqp://guest:guest@localhost:5672/
        run: go test -tags=integration -timeout=300s ./... -v -cover

  build-and-push:
    name: Build & Push Docker Image
    needs: quality-assurance
    runs-on: ubuntu-latest
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.DOCKER_IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' }}
            type=sha

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and Push
        uses: docker/build-push-action@v5
        with:
          context: ${{ env.SERVICE_PATH }}
          file: ${{ env.SERVICE_PATH }}/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=registry,ref=${{ env.DOCKER_IMAGE_NAME }}:buildcache
          cache-to: type=registry,ref=${{ env.DOCKER_IMAGE_NAME }}:buildcache,mode=max