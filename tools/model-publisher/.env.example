# HuggingFace Hub
HF_TOKEN=hf_your_write_token_here
HF_USERNAME=your_huggingface_username

# Local artifact resolution (assets/<model> under PROJECT_ROOT_DIR)
PROJECT_ROOT_DIR=.
ASSETS_DIR_NAME=assets

# Optional: override dotenv file when using provider
# ENV_FILE=.env
