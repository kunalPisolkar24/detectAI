# Standalone postgres-users (infra/docker/postgres-users/standalone.yml).
# Copy to infra/docker/postgres-users/.env when running standalone, or reuse
# infra/docker/data/.env (same variable names). Importing stacks
# (e.g. apps/web `make start`) supply values via their own env file;
# only the datastore connection vars below matter there. See
# infra/docker/redis-users/standalone.yml and infra/docker/rabbitmq/standalone.yml
# for the same pattern.
COMPOSE_PROJECT_NAME=detect_ai_postgres_users

POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=detect_ai
POSTGRES_PORT=5432
