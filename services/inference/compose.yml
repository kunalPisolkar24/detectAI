services:
  ai-service:
    container_name: detect_ai_inference
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "/bin/grpc_health_probe", "-addr=:50051"]
      interval: 30s
      timeout: 10s
      retries: 3
    ports:
      - "${PORT_INFERENCE}:50051"
      - "127.0.0.1:${PORT_INFERENCE_METRICS:-8333}:8333"
    networks:
      - detectai_net
    environment:
      - PYTHONUNBUFFERED=1
      
      - ENV=${NODE_ENV}
      - GRPC_PORT=${PORT_INFERENCE:-50051}
      - METRICS_PORT=${PORT_INFERENCE_METRICS:-8333}
      - API_KEY=${AI_SERVICE_API_KEY}
      - MODEL_CACHE_DIR=${INFERENCE_MODEL_CACHE_DIR:-./models}
      - BATCH_SIZE=${INFERENCE_BATCH_SIZE:-32}
      - BATCH_TIMEOUT=${INFERENCE_BATCH_TIMEOUT:-0.05}
      - MAX_TEXT_LENGTH=${INFERENCE_MAX_TEXT_CHARS:-50000}
      - BATCH_QUEUE_MAX_SIZE=${INFERENCE_BATCH_QUEUE_MAX_SIZE:-1024}
      - MAX_INFLIGHT_DOC_CHUNKS=${INFERENCE_MAX_INFLIGHT_DOC_CHUNKS:-8}
      - CHUNK_TOKEN_LIMIT=${INFERENCE_CHUNK_TOKEN_LIMIT:-256}
      - CHUNK_TOKEN_STRIDE=${INFERENCE_CHUNK_TOKEN_STRIDE:-192}
      - INFERENCE_PROVIDERS=${INFERENCE_PROVIDERS:-CUDAExecutionProvider,CPUExecutionProvider}
      - GRPC_MAX_WORKERS=${INFERENCE_GRPC_MAX_WORKERS:-50}
    deploy:
      resources:
        limits:
          memory: 8G
          cpus: "4.0"
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]

networks:
  detectai_net:
    name: ${detect_ai_network}
    external: true
